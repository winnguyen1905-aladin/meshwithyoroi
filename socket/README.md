# Socket.IO Base Structure

This directory contains the base structure for Socket.IO integration (client-only). Files are placeholders without implementation logic.

Structure:
- `index.ts` – entry point for socket exports
- `constants.ts` – shared constants for socket layer
- `types.ts` – shared TypeScript types for socket layer
- `client/index.ts` – client-side socket entry


